{
  "private": true,
  "scripts": {
    "clean": "rimraf .pulp-cache output",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "pulp": "^13.0.0",
    "purescript": "^0.13.2",
    "purescript-psa": "^0.7.3",
    "rimraf": "^3.0.0"
  }
}
